import { cn } from "@/lib/utils"

// Skeleton component to display a loading placeholder with pulse animation
function Skeleton({
  className,
  ...props
}) {
  return (
    <div
      data-slot="skeleton"
      className={cn("bg-accent animate-pulse rounded-md", className)}
      {...props} />
  );
}

export { Skeleton }
